apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Aggregates the namespace and all environment-agnostic services.
# Overlays reference this whole directory:  ../../base
resources:
  - namespace.yaml
  - networkpolicy-default-deny.yaml
  - postgres
  - redis
  - api
