apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: droplet

resources:
  - statefulset.yaml
  - service.yaml
  - pvc.yaml
  - networkpolicy.yaml

configMapGenerator:
  - name: redis-config
    files:
      - files/redis.conf
